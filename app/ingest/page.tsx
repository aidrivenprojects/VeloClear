"use client";
import {useMemo,useState} from "react";import {Shell} from "@/components/Shell";import {Card} from "@/components/ui";
export default function Page(){const[text,setText]=useState("");const steps=useMemo(()=>text?["Capture workflow","Identify users","Create backlog","Create risks","Define governance"]:[],[text]);return <Shell title="Workflow Ingestion" kicker="Create"><div className="grid gap-6 xl:grid-cols-2"><Card className="p-8"><h2 className="text-3xl font-black text-ink">Paste a workflow.</h2><textarea value={text} onChange={e=>setText(e.target.value)} className="mt-5 min-h-[280px] w-full rounded-2xl border border-border bg-surface p-4 text-sm font-bold outline-none"/></Card><Card className="p-8"><h3 className="text-xl font-black text-ink">Generated structure</h3><div className="mt-4 grid gap-3">{steps.map(s=><div key={s} className="rounded-2xl bg-surface p-3 text-sm font-black text-ink">✓ {s}</div>)}</div></Card></div></Shell>}
