"use client";
import Link from "next/link";import {Shell} from "@/components/Shell";import {Card,Badge} from "@/components/ui";import {listProjects} from "@/lib/store";
function tone(v:string){return v==="red"?"red":v==="green"?"green":"amber"}
export default function Page(){const projects=listProjects();return <Shell title="Projects" kicker="Portfolio"><div className="space-y-6"><Card className="p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-xs font-black uppercase tracking-wider text-accent">Project Registry</div><h2 className="mt-2 text-3xl font-black tracking-[-0.04em] text-ink">Select a project or create a new one.</h2></div><Link href="/new-project" className="rounded-xl bg-accent px-5 py-3 text-sm font-black text-white">+ New Project</Link></div></Card><div className="grid gap-5 lg:grid-cols-3">{projects.map(p=><Link key={p.id} href={`/projects/${p.id}`}><Card className="h-full p-5 transition hover:-translate-y-0.5 hover:border-accent/50"><div className="flex items-start justify-between gap-3"><div><div className="text-xs font-black uppercase tracking-wider text-muted">{p.programme}</div><h3 className="mt-2 text-xl font-black text-ink">{p.name}</h3></div><Badge tone={tone(p.health)}>{p.health}</Badge></div><p className="mt-3 text-sm leading-6 text-ink2">{p.summary}</p><div className="mt-5 text-xs font-black text-accent">Open workspace →</div></Card></Link>)}</div></div></Shell>}
