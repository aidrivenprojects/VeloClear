# Demo Click Fix

The demo page now uses normal Next.js links instead of local state-only buttons.

Fixed:
- Programme Workspace → /projects/new
- Portfolio Demo → /dashboard
- View workspace → /dashboard
- Trace causes → /impact-trace
- Trigger governance → /raid
- Learn patterns → /dashboard
- Role cards route to relevant app pages
- Upload/connect panels route to /integrations

This makes the demo reliable on Vercel and easier to test.
