import {Card} from "./Card";import {Badge} from "./Badge";export function MetricCard({label,value,note,tone="indigo"}:{label:string;value:string;note:string;tone?:"indigo"|"green"|"amber"|"red"|"slate"}){return <Card className="p-5 transition hover:-translate-y-0.5 hover:shadow-lift"><div className="flex items-center justify-between"><div className="rounded-xl bg-accentBg px-3 py-2 text-xs font-black text-accent">{label.slice(0,2).toUpperCase()}</div><Badge tone={tone}>{note}</Badge></div><div className="mt-4 font-mono text-3xl font-medium tracking-tight text-ink">{value}</div><div className="mt-1 text-[10px] font-black uppercase tracking-wider text-muted">{label}</div></Card>}