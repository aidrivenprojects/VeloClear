import {demoProjects} from "@/lib/demoData";import {Card} from "@/components/ui/Card";import {Badge} from "@/components/ui/Badge";function tone(t:string){return t==="green"?"green":t==="red"?"red":"amber"}export function ProjectHealthList(){return <Card><div className="border-b border-border p-5"><h3 className="text-sm font-black">Portfolio RAG Summary</h3><p className="text-xs text-muted">Live portfolio status</p></div>{demoProjects.map(([n,r,s,t])=><div key={n} className="flex items-center justify-between border-b border-border px-5 py-4 last:border-0"><div><div className="text-sm font-black">{n}</div><div className="mt-1 text-xs text-muted">{s}</div></div><Badge tone={tone(t) as any}>{r}</Badge></div>)}</Card>}