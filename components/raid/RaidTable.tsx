import {Card} from "@/components/ui/Card";import {Badge} from "@/components/ui/Badge";const raid=[["R001","Vendor integration milestone may slip","Vendor misses handoff by 2 working days","Mitigate","red"],["R002","Capacity may not support committed work","Capacity below 80% for 2 weeks","Monitor","amber"],["D001","External API dependency","Blocked milestone","Track","indigo"]];export function RaidTable(){return <Card><div className="border-b border-border p-5"><h2 className="text-sm font-black">RAID with trigger logic</h2><p className="text-xs text-muted">Observable triggers make governance automatic.</p></div>{raid.map(([id,title,trigger,action,tone])=><div key={id} className="grid gap-4 border-b border-border p-5 text-sm last:border-0 lg:grid-cols-[80px_1fr_1fr_120px]"><strong>{id}</strong><span>{title}</span><span className="text-muted">{trigger}</span><Badge tone={tone as any}>{action}</Badge></div>)}</Card>}