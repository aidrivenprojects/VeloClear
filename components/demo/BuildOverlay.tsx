"use client";import {useEffect,useState} from "react";const steps=["Selecting role-aware navigation","Generating starter dashboards","Linking delivery intelligence","Preparing narrative surfaces"];export function BuildOverlay({open,roleLabel}:{open:boolean;roleLabel:string}){const[done,setDone]=useState(0);useEffect(()=>{if(!open){setDone(0);return}const t=setInterval(()=>setDone(c=>Math.min(c+1,steps.length)),360);return()=>clearInterval(t)},[open]);if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-sidebar/95 p-6 text-white"><div className="w-full max-w-xl rounded-[28px] border border-white/10 bg-white/5 p-8 text-center shadow-modal"><div className="mx-auto mb-4 grid h-12 w-12 place-items-center rounded-2xl bg-accent text-sm font-black">VC</div><h2 className="text-3xl font-black tracking-[-0.04em]">Building your delivery workspace…</h2><p className="mt-2 text-sm text-white/60">Preparing {roleLabel} workspace.</p><div className="mt-6 grid gap-3 text-left">{steps.map((s,i)=><div key={s} className={i<done?"rounded-2xl bg-accent/30 px-4 py-3 text-sm font-bold text-white":"rounded-2xl bg-white/5 px-4 py-3 text-sm font-bold text-white/50"}>{i<done?"✓ ":"○ "}{s}</div>)}</div></div></div>}
